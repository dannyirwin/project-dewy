# CLAUDE.md

@AGENTS.md

All agent instructions live in [AGENTS.md](./AGENTS.md) (canonical, tool-agnostic). Read it before making changes. Architecture decisions: `docs/adr/`. Definition of done: `pnpm typecheck && pnpm lint && pnpm test && pnpm eval`.
